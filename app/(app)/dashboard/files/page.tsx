import DashboardFiles from '@/components/dashboard/DashboardFiles';

// Server component wrapper for the Files page.
// All client-side interactions with the API live inside DashboardFiles.
export default function FilesPage() {
  return <DashboardFiles />;
}
